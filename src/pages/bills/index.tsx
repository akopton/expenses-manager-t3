import Head from "next/head";
import Link from "next/link";

export default function BillsPage() {
  const links = [
    { id: 1, name: "Kategorie", href: "/bills/categories" },
    { id: 2, name: "Zestawy", href: "/bills/sets" },
  ];

  return (
    <>
      <Head>
        <title>Dashboard</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="grid h-screen grid-cols-2 grid-rows-1 gap-20 p-20">
        {links.map((link) => {
          return (
            <Link
              href={link.href}
              key={link.id}
              className=" flex h-full w-full flex-col items-center justify-center gap-20 rounded-4xl border-2 text-4xl shadow-primaryColor transition-all hover:scale-95"
            >
              <span>{link.name}</span>
              <span>Ilość pozycji:</span>
            </Link>
          );
        })}
      </main>
    </>
  );
}

{
  /*  */
}
